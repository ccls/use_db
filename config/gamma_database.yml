
# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
gamma_development:
  adapter: mysql
  encoding: utf8
  database: use_db_gamma_development
  username: root
  password:
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
gamma_test:
  adapter: sqlite3
  pool: 5
  timeout: 5000
  database: db/use_db_gamma_test.sqlite3
#  adapter: mysql
#  encoding: utf8
#  database: use_db_gamma_test
#  username: root
#  password:
#  host: localhost


#	unfortunately, the current use_db technique does not allow for
#	different adapters in a 'family' of databases.
#	I'm gonna try to make it more like a schema dump which may
#	not have actually existed when this was written.


gamma_production:
  adapter: mysql
  encoding: utf8
  database: use_db_gamma_production
  username: root
  password:
  host: localhost

