
# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
gamma_development:
  adapter: mysql
  encoding: utf8
  database: use_db_gamma_development
  username: root
  password:
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
gamma_test:
  adapter: mysql
  encoding: utf8
  database: use_db_gamma_test
  username: root
  password:
  host: localhost

gamma_production:
  adapter: mysql
  encoding: utf8
  database: use_db_gamma_production
  username: root
  password:
  host: localhost

